# 1.1.0 — As-Built

> Differences between plan and what shipped, with why. Source of truth: the code.
> Release 1.1.0 = topic `15-landing-page`.

Shipped as planned, no deviations: public `/` route renders `LandingPage`
(rotating job-portal animation, static kanban preview, i18n PL/EN, mobile
responsive); authenticated users redirect to `/dashboard`.
