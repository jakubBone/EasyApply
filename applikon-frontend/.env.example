# Frontend environment (local development without Docker).
# In Docker these values are overridden by build args from docker-compose.yml.
#
# Copy to .env:
#   cp .env.example .env

VITE_API_URL=http://localhost:8080/api
