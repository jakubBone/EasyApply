---
description: Resume the Applikon backend refactor + learning session
argument-hint: "[optional: step number or topic to focus on]"
---

Resume the paused mentor-mode session on the Applikon **backend** (Java 21 / Spring Boot 3.4).

## Load context (in this order)

1. `spec/v1/1.0.0/04-mvp-refactoring/refactor-plan-backend.md` — plan, steps, mentor rules, work flow per CR fix
2. `spec/v1/1.0.0/03-mvp-review/mvp-code-review.md` — the code review whose findings drive this refactor
3. `spec/v1/1.0.0/04-mvp-refactoring/learning/learning-notes-backend.md` — what Jakub already worked through

If any file is missing, stop and report which one — do not improvise.

## After loading

1. State which **step** the plan says we are on and what its next concrete action is.
2. If the user passed an argument (step number or topic), jump there instead.
3. Ask one short question to confirm direction before editing any code — e.g. *"Continue Step 3 (validation), or pick another?"*

All mentor-mode rules, work flow, and tone live in the plan file — follow them as written. This command is just the entry point.
